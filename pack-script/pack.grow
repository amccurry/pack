#!/bin/bash
set -e
if [ "$#" -ne 2 ]; then
  echo "Usage:"
  echo "${0} <volumename> <new length>"
fi

VOLUME_NAME="${1}"
SIZE="${2}"
curl -d "{\"sizeInBytes\":${SIZE}}" -X POST localhost:4567/api/v1.0/grow/${VOLUME_NAME} 2>/dev/null | jq -r '.'
